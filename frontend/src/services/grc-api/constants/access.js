const USER_ROLES = ['super_admin', 'admin', 'manager', 'assessor', 'viewer'];

const USER_STATUSES = ['active', 'inactive', 'locked', 'suspended'];

module.exports = {
  USER_ROLES,
  USER_STATUSES,
};
